// Пример 4 - с помощью метода split() и метода join() напишите функцию snakeToCamelCase(word)
// которая принимает имя переменной в формате snake_case и возвращает новое имя, в формате camelCase

const variableName = 'this_is_some_variable_name';

const snakeToCamelCase = (word) => {
  
};

console.log(variableName);
console.log(snakeToCamelCase(variableName));